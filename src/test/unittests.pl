#!/usr/bin/env perl
use strict;
use FindBin qw($RealBin);
use lib "$RealBin";
use lib "$RealBin/..";
use lib "$RealBin/../deps";
use lib "$RealBin/../auto/XSTools";
use lib "$RealBin/../..";

use List::MoreUtils;

1;
